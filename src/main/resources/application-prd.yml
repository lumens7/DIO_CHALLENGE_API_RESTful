spring:
  datasource:
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSOWRD}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: created
